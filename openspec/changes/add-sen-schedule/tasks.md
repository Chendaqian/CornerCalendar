## 1. Markdown 契约与模型

- [x] 1.1 使用用户提供的 14 行表格，为表头识别、合法解析、行号错误、闭区间天数、TR5 日期并集和未知序号编写失败优先的单元测试
- [x] 1.2 新增森迭代与活动模型，并为 `AppSettings` 增加默认关闭的显示开关和迭代集合，验证旧设置 JSON 的兼容加载
- [x] 1.3 实现严格五列 Markdown 表格解析器，使有效样例得到 14 行结构化活动、无效输入整表失败且包含具体行号

## 2. 迭代规则与查询

- [x] 2.1 实现序号 1–14 的 TR、常规角标、末日“提”角标和八个圆圈分组映射，并让未知序号安全降级为无特殊视觉的森活动
- [x] 2.2 实现活动闭区间自然日数、按周末/休/班计算工作天和同迭代 TR 覆盖日期并集，验证 TR3、序号 10/11 与 TR5 的预期总天数
- [x] 2.3 实现多迭代日期范围查询、同日主活动确定性选择和按迭代分组的提示数据生成

## 3. 日历数据接入

- [x] 3.1 调整 `CalendarViewModel`，仅在森开关开启时把已导入迭代合并到月历和近期事件数据
- [x] 3.2 实现 ViewModel 的森日程即时切换与设置重载，确保不触发网络请求且关闭后普通 ICS、中国日历和“班/休”不受影响
- [x] 3.3 补充 ViewModel 回归测试，覆盖默认关闭、开关往返、多个迭代同日和删除迭代

## 4. 设置页导入与管理

- [x] 4.1 在左侧分类导航新增“森日程”，添加全局开关、迭代名称单行文本框、Markdown 多行文本框和“导入/更新”按钮
- [x] 4.2 实现导入校验和错误展示，同名迭代确认后完整更新，不同名称新增
- [x] 4.3 实现已导入迭代列表、日期范围展示与确认删除，并接入设置加载、保存和恢复默认

## 5. 主窗口与日期格

- [x] 5.1 在刷新按钮旁新增固定尺寸“森”按钮，实现明确状态、工具提示和返回 `Task` 的异步切换处理
- [x] 5.2 在日期格左上角新增 18×18 森角标层，实现 R1–R5、宣、反、编、提、测、上、巡、总和末日覆盖
- [x] 5.3 新增阶段圆圈层，实现序号 1–4 的 TR1–TR4 色、5–9 的 TR5 色、10 编码色、11 测试色、12–14 上线阶段色；周末、“班”和“休”不画圆圈，并保持蓝色选中态优先
- [x] 5.4 扩展日期悬浮提示，按迭代名称列出紧凑 TR/活动自然日数、工作天、当前活动和同日多个迭代
- [x] 5.5 在 Light/Dark 主题增加八个阶段色和专属角标色，使基础角标与阶段圆圈同色、专属角标优先自身颜色且不复用“班/休”画刷
- [x] 5.6 将日历主窗口及日期相关子窗口外框和根内容按 1.1 比例等比放大；设置窗口保持原尺寸，验证日期格、文字和控件布局不遮挡
- [x] 5.7 实现日期详情同日点击切换、跨日期复用，以及天气窗口左侧顶部对齐和再次点击关闭；无阶段圆圈日期不显示森角标

## 6. 验证与交付

- [ ] 6.1 执行 `dotnet test src\CornerCalendar.sln`，修复所有新增与既有测试失败
- [ ] 6.2 执行 `dotnet build src\CornerCalendar.sln`，确认 0 错误 0 警告且无无关格式或生成文件改动
- [ ] 6.3 在 Windows Light/Dark 下手工检查 Markdown 导入/同名更新/删除、多个迭代、默认关闭、森按钮、角标与班休并存、八种圆圈、蓝色选中态和包含迭代名称的提示
